# Base URL of the backend API, as reachable from the BROWSER (host machine).
# Using localhost here is correct and unavoidable — it is not a bug, only
# the Docker-internal service traffic (backend -> db, backend -> s3) must
# avoid localhost.
VITE_API_URL=http://localhost:5000/api
